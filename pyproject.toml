[project]
name = "comfyui-cauce"
version = "1.2.0"
description = "Continuity, temporal inpainting, and motion-field operations for ComfyUI"
readme = "README.md"
requires-python = ">=3.10"
license = { file = "LICENSE" }
authors = [
  { name = "Hypereikon" }
]
dependencies = []

[project.urls]
Homepage = "https://hypereikon.online"
Repository = "https://github.com/hypereikon-lab/ComfyUI-Cauce"

[tool.comfy]
PublisherId = "hypereikon-lab"
DisplayName = "CAUCE"
Icon = ""

[tool.ruff]
line-length = 100
target-version = "py310"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["cauce", "cauce_nodes"]
