{
  "1": {
    "class_type": "CauceGenerationWindow",
    "inputs": {
      "window_id": "{{window.id}}",
      "accepted_start_seconds": "{{window.accepted_start_seconds}}",
      "accepted_duration_seconds": "{{window.accepted_duration_seconds}}",
      "context_frames": "{{window.context_frames}}",
      "duplicate_prefix_frames": "{{window.duplicate_prefix_frames}}",
      "snap_mode": "ceil",
      "accept_mode": "{{window.accept_mode}}",
      "maximum_frames": 362
    }
  }
}
